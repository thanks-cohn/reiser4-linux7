# Reiser4-Linux7 Failure Registry

This registry tracks known failures until a gate script proves them fixed. Log paths should point at files under `artifacts/` when available.

| Failure | Symptom | Trigger command | Suspected function | Current status | Next step | Log/artifact path |
| --- | --- | --- | --- | --- | --- | --- |
| mkdir EPERM | `mkdir` on a mounted Reiser4 image returns `EPERM` / operation not permitted. | `sudo mkdir <mountpoint>/dir1` or `scripts/reiser4-v3-mkdir-regression.sh` | `plugin/inode_ops.c::reiser4_mkdir_common()` and `do_create_vfs_child()` plugin/estimate/creation path | Open blocker; instrumentation added, but exact returning branch must be confirmed on a kernel-capable host. | Run the V3 mkdir regression, inspect `BUMRUSH26_MKDIR_*` tags, then patch only the confirmed branch. | TBD |
| assign_conversion_mode / convert_ctail NULL dereference | Kernel NULL dereference in ctail conversion path. | Workload that reaches tail conversion / ctail conversion. | `plugin/item/ctail.c::assign_conversion_mode()` / `convert_ctail()` | Open blocker outside the current safe V3 path. | Preserve crash log, identify required preconditions, and add a focused regression before patching. | TBD |
| inode 65536 eviction | Eviction leaves pages attached; `clear_inode()` is unsafe and currently guarded by temporary return. | Unmount after basic loopback IO. | `super_ops.c::reiser4_evict_inode()` | Open teardown blocker; detailed eviction instrumentation is present. | Use folio/page state logs to fix the remaining page/private reference before allowing `clear_inode()`. | `docs/progress/report/2026-05-29-eviction-status.md` |
| clean unmount failure | Unmount fails, hangs, warns, or leaves dirty/in-use state. | `sudo umount <mountpoint>` after smoke or mkdir regression. | `super_ops.c::reiser4_evict_inode()`, writeback, jnode/page invalidation, daemon shutdown | Open blocker. | Run strengthened V3 mkdir and V3 proof gates; compare pre/post `dmesg` and mount state. | TBD |
| module unload refs / entd / ktxnmgrd | `rmmod reiser4` fails due to references or lingering kernel threads. | `sudo rmmod reiser4` after unmount. | `reiser4_put_super()`, `entd`, `ktxnmgrd`, reference teardown | Open blocker. | Capture `lsmod`, thread state, mount state, and `dmesg`; fix leaked refs before V3. | TBD |

## Smoke Harness Failure Entries

| Failure | Symptom | Reproduction command | Expected breadcrumb | Artifact location | Current status | Next target |
| --- | --- | --- | --- | --- | --- | --- |
| `MKDIR_EPERM_CURRENT_BLOCKER` | `mkdir` on a mounted Reiser4 image fails with `EPERM` / operation not permitted. | `sudo tests/smoke_mkdir_only.sh` or `sudo tests/smoke_reiser4_v1.sh` | `SMOKE_MKDIR_FAIL error="..."`; V1 includes `SMOKE_MKDIR_FAIL path=<path> error="..."`; filtered dmesg should preserve `BUMRUSH26_MKDIR` if present. | `artifacts/smoke_mkdir_only-<timestamp>/` and `artifacts/smoke_reiser4_v1-<timestamp>/` | Open current V1 blocker; expected to fail until the confirmed mkdir branch is fixed. | Inspect `dmesg-after-mkdir-failure.txt`, `dmesg-after-mkdir-failure-filtered.txt`, and `bumrush26-mkdir.txt`; patch only the confirmed returning branch. |
| `TEARDOWN_MODULE_REF_STUCK_AFTER_FAILED_SMOKE` | After a failed smoke path, `rmmod reiser4` reports the module is in use or `lsmod` shows a nonzero/stuck use count. | `sudo tests/smoke_teardown_after_failure.sh` | `SMOKE_TEARDOWN_AFTER_FAIL_FAIL module_stuck=1 ...` or `SMOKE_RMMOD_FAIL module_ref_stuck=1`; library cleanup may print `REISER4_TEARDOWN_FAIL module_still_loaded=1`. | `artifacts/smoke_teardown_after_failure-<timestamp>/` | Open teardown risk after failed operations. | Compare before-failure, after-failure, and after-cleanup state snapshots; identify leaked mount/inode/daemon references. |
| `LOOP_DEVICE_DELETED_IMAGE_STUCK` | `losetup -a` shows a Reiser4 test image, including `/tmp/reiser4-*.img (deleted)`, still attached after cleanup. | `sudo tests/smoke_teardown_after_failure.sh` or any failing smoke test followed by `losetup -a` | `SMOKE_LOOP_STUCK_DELETED_IMAGE`, `SMOKE_LOOP_STUCK deleted_image=1`, or `REISER4_TEARDOWN_FAIL deleted_loop_image=1`. | `artifacts/<smoke-test>-<timestamp>/state-after-cleanup.txt` | Open cleanup symptom observed after failed V1 smoke. | Determine whether failed unmount prevents loop autoclear; detach known test loops and fix the filesystem state that blocks unmount. |
| `KTXNMGRD_STUCK_AFTER_FAILURE` | `ps -ef` shows `ktxnmgrd` or `entd` after smoke cleanup. | `sudo tests/smoke_teardown_after_failure.sh` or `sudo tests/smoke_reiser4_v1.sh` | `SMOKE_KTXNMGRD_STUCK` or `REISER4_TEARDOWN_FAIL ktxnmgrd_alive=1`. | `artifacts/<smoke-test>-<timestamp>/state-after-cleanup.txt` | Open teardown/thread-shutdown risk. | Trace daemon lifetime through failed operation cleanup and module removal; ensure shutdown is not blocked by leaked transactions/references. |
| `INSMOD_FILE_EXISTS_WHEN_MODULE_PRELOADED` | `insmod ./reiser4.ko` fails because `reiser4` is already loaded, often reported as file exists. | `sudo tests/smoke_module_lifecycle.sh` or `sudo tests/smoke_reiser4_v1.sh` with a preloaded module | `SMOKE_PREFLIGHT_FAIL module_preloaded=1`; V1 exits unless `REISER4_ALLOW_PRELOADED=1` is explicitly set. | `artifacts/smoke_module_lifecycle-<timestamp>/` or `artifacts/smoke_reiser4_v1-<timestamp>/` | Guarded by smoke preflight to prevent confusing downstream failures. | Start from a clean boot or manually unload/fix the stuck preloaded module before running filesystem tests. |
